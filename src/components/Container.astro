---
interface Props {
  width?: "default" | "wide";
}

const { width = "default" } = Astro.props;

const maxWidth = width === "wide" ? "max-w-[1200px]" : "max-w-[1024px]";
---

<div class:list={["mx-auto px-6 md:px-0", maxWidth]}>
  <slot />
</div>
